1. this should be run after generate swarm key on master node and peer id on each node
2. swarm.key is generated on master node then sent to other nodes
3. peer id for each nodes should be added to files/addnodes.sh
